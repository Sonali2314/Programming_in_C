//Write a Bitwise AND of two numbers . 
#include <stdio.h> 
int main() { 
 int a, b; 
 printf("Enter the first integer: "); 
 scanf("%d", &a); 
 printf("Enter the second integer: "); 
 scanf("%d", &b); 
 int result = a & b; 
 printf("Binary AND result: %d\n", result); 
 return 0; 
} 
